from utils import db
import main


def menu_ajouter_donnees(conn):
    """
    Menu d'ajout des données

    :parem conn: Connexion à la base de données
    """
    print("La fonction d'ajout est à implémenter")
